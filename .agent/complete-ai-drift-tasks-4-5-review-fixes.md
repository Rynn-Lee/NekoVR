Your implementation does not pass independent review. Continue the same conversation and redo the incomplete parts below. First change tasks 4.1-4.8 and 5.1-5.7 back to unchecked while fixing; re-check each only after the acceptance criteria and real tests pass. Do not touch tasks 6+ or unrelated prior dirty changes.

RESET/DATASET DEFECTS (all must be fixed):
1. Reset lifecycle and label data currently exist only in memory. Canonical `nekovr_dataset_v1.fbs` ResetLabel/DatasetEvent serialization loses requestMonotonicNs, appliedMonotonicNs, stable lifecycle/request ID, source/outcome/body parts, per-tracker raw + calibrated-pre-AI orientations, complete pre/post adjustment chain, HMD references/validity, epochs, and reset domain. Extend the canonical versioned schema/contracts/bindings/readers and conformance tests so these fields are actually persisted and decoded. Preserve compatibility using additive fields/minor version as appropriate.
2. Every ResetLabelRecord has eventIndex=0. Allocate a stable unique event index and make each tracker label point to its real APPLIED reset event. REQUESTED/CANCELLED/FAILED/APPLIED records for one request need a shared request/lifecycle ID and the true event request/apply monotonic times (relative to session start when serialized), not `clockNs()` receipt time.
3. `Tracker.snapshotResetState()` reads lastPreAiRotation before getRotation() updates it, and label correction uses final AI-adjusted `adjustedOrientation`. Capture a fresh calibrated pre-AI orientation deterministically and derive `q_target = normalize(q_post_preAI * inverse(q_pre_preAI))`; do not mix removal/application of AI into reset ground truth. Persist final output separately only if needed.
4. Quality flags PACKET_GAPS, RECONNECT_OR_REASSIGNMENT, OVERLAPPING_RESETS are declarations only; no production call sets them. Compute them from tracker counters/topology/status changes and the actual pre/post context window. HMD validity must include finite/nonzero and staleness/sample age. Motion/invalid/context flags must represent the window, not only unused optional defaults. Add tests proving each flag is raised from real recorder/reset inputs.
5. Context window behavior needs deterministic configurable pre/post frame counts, unique event linking, correct ordered inclusive/exclusive semantics, and both start/end truncation + insufficient-context flags. Add archive encode/decode tests proving labels reference existing frames and preserve pre/post snapshots/times.
6. Mounting reset and clearMounting increment epochs but do not clear/rebase legacy drift; fix it. AI results should carry the tracker epoch so pre-reset results are always rejectable; do not special-case epoch 0 as permanently acceptable. Add a fake delayed source test that proves an old result cannot be applied after reset. Also remove the remaining `VRServer.instance` dependency from TrackerResetsHandler.refreshDriftCompensationEnabled via injection/explicit ownership as required by reset instrumentation and the already-checked baseline.
7. Tests currently do not actually cover a failed reset, delayed reset application timing, mounting known-transform persistence, serialized event linkage/snapshots, or production quality-flag derivation. Add meaningful fixtures, not manually publishing the event you then assert.

RPC/SERVER DEFECTS:
8. RPC schema has string errors and numeric severities, no typed error enum, no STARTING/RECOVERABLE/CANCELLED semantics, no elapsed/finalization/disk-free metrics, no active roster details, no validation finding list/state, and weak session correlation (Stop has no session ID). Extend schema with typed operation/error/readiness/validation enums and all required authoritative fields. Existing RpcMessageHeader.tx_id may be the request ID if consistently echoed; every command response must correlate and include authoritative session ID.
9. `stopAndFinalize` is queued onto the tracking/server queue and can block it for up to timeout. Move finalization to a dedicated worker/executor. Broadcast ordered FINALIZING before work, then COMPLETED/FAILED after; broadcast STARTING/RECORDING and cancellation too. Ensure multi-client observers see transitions, not merely start/completed. Add reconnect and ordered multi-client tests.
10. Cancel currently finalizes, deletes the archive, and leaves COMPLETED with a dangling output path. Implement a real recorder cancellation/abort that closes the writer safely, removes only the working partial for that active session, and produces an authoritative cancelled/idle state without presenting a completed archive.
11. Inventory marks any parseable manifest as valid and resetCount=0. Run DatasetArchiveValidator for every listed archive (or cached validated result), expose actual validation state/findings and actual manifest quality/reset/roster summary. List only validated completed or explicit recoverables. Never fabricate valid status.
12. Recovery with quarantine=false returns 'requires quarantine'; that is not recovery. Implement the recoverable path already promised by tasks 3.9/5.1, or model recovery vs quarantine as separate typed actions with truthful capability/error. Add success/failure tests.
13. Server export accepts an arbitrary renderer-provided absolute path and creates directories/copies there. This violates the security boundary. Do not accept arbitrary destination paths over RPC. Use an Electron main-process save dialog grant for destination; server RPC may validate/identify the managed source. Electron reveal/export channels must accept a sanitized session ID (not an arbitrary path), derive the managed source, resolve real/canonical existing root/source (including symlink/junction escape protection), and perform reveal/copy. Delete/recover must similarly use canonical root checks and must not follow links outside root. Add tests against traversal and symlink/junction escape where supported.
14. `normalize().startsWith()` / `path.relative()` alone is not canonical containment. Use realpath for existing roots/sources, no-follow/link checks where destructive, and canonical parent checks for newly created targets. Ensure Electron and server use exactly the same managed datasets root in packaged and dev modes; `process.cwd()/datasets` is not a robust packaged root unless server is explicitly configured to it.
15. Protocol `tsconfig.json` must not resolve flatbuffers from `../gui/node_modules`. Remove that consumer-specific hack; use/install the protocol package's declared dependency and make `pnpm/npm build` reproducible from the protocol project. Regenerate Java and TypeScript from rpc.fbs through the normal generator. Do not merely copy stale dist into pnpm cache as the source of correctness.

GUI/TEST DEFECTS:
16. Widget bypasses DatasetRevealRequest and directly passes server path to Electron; exported/reveal hook methods are unused and there is no Export or Cancel UI. Wire actual RPC acknowledgement followed by typed Electron session-ID IPC. Surface typed action errors.
17. UI omits required live queue depth/high watermark, free/used disk, active roster, finalization progress, validation findings, and truthful archive inventory details. Add them using server fields, with no fabricated 50-Hz elapsed time if the server can report actual elapsed monotonic duration.
18. Starting silently sends `consent=true`; add an explicit accessible consent control and subject/privacy/profile choices. Keep production-profile collection unavailable until the later dataset-ready gate (task 6.4); development/minimum capture may be clearly labelled if appropriate. Never imply dataset-ready now.
19. Localize every visible string including Error/Warning, ID, frames/trackers, confirmation/cancel/export/action failures. Add accessible names for icon-only delete/confirm/cancel controls and correct focus/confirmation behavior.
20. `gui/src/tests/dataset-recorder.test.ts` is mostly DTO property assertions and reimplements a fake containment function. It does not test React hook/widget, Electron handlers, WebSocket lifecycle, reconnect, start/stop/finalize/reveal, or request correlation. Replace/add real tests against extracted production state reducer/IPC helpers and component/hook behavior. Wire them into a normal `gui` test script/gate; do not rely on a one-off manual node command. Server multi-client tests must assert ordered broadcasts including FINALIZING and FAILED/CANCELLED, and wrong-session/conflicting requests.

After fixes, run and report exact results for: protocol generation and standalone protocol TypeScript build without GUI-node_modules path mapping; focused reset serialization/quality/epoch tests; focused RPC state/security/recovery tests; real GUI tests; `pnpm -C gui lint`; `pnpm -C gui build`; full `:server:core:test`. Do not claim pass until commands have completed. Then mark only 4.* and 5.* checked.
