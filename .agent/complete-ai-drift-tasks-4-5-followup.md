Continue the most recent conversation and finish the assigned OpenSpec tasks 4.1-4.8 and 5.1-5.7. The previous print session timed out while rerunning DatasetRPCHandlerTests; no task checkbox is marked yet, which is correct because work is incomplete.

First inspect the current workspace and the exact failing output/report for DatasetRPCHandlerTests. Fix the root cause; do not just rerun indefinitely or weaken assertions. Then complete all remaining server RPC, React hook/widget, Electron safe operations, English/Russian Fluent strings, and meaningful reconnect/multi-client/GUI lifecycle tests. Ensure schema covers every required operation including cancel and reveal (not just start/stop/list). Ensure authoritative status events reach all clients and request/session IDs remain correlated. Ensure file operations canonicalize paths under managed roots.

Run focused tests as you go. Before finishing, run relevant reset and RPC suites, full :server:core:test, pnpm -C gui lint, production GUI build, protocol TypeScript compile/generation checks, and new GUI tests. Fix failures caused by your changes. Do not touch 6.* or later tasks, do not commit, and preserve prior dirty changes. Only mark each 4.* and 5.* task complete after its implementation and test evidence exists. Return a precise task mapping and commands/results.
